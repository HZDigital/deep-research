name: Trigger auto deployment for deep-research-mcp-staging

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ staging ]
    paths:
    - '**'
    - '.github/workflows/deep-research-mcp-staging-AutoDeployTrigger-bf50c588-814c-4160-931b-8228581cd5d9.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.DEEPRESEARCHMCPSTAGING_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.DEEPRESEARCHMCPSTAGING_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.DEEPRESEARCHMCPSTAGING_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: hzgenaicontainerregistry.azurecr.io
          registryUsername: ${{ secrets.DEEPRESEARCHMCPSTAGING_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.DEEPRESEARCHMCPSTAGING_REGISTRY_PASSWORD }}
          containerAppName: deep-research-mcp-staging
          resourceGroup: AIWebinarRAG01_RG
          imageToBuild: hzgenaicontainerregistry.azurecr.io/deep-research-mcp-staging:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


